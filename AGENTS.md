# AGENTS.md

このリポジトリで働くエージェント（Zed / Claude Code / Codex）向けの共通ルール。

## スキル

詳細な作業手順はスキルとして `.agents/skills/` に置いてある。該当する場面では必ず参照すること。

- `analysis-integrity` — 複数文書にまたがる分析の整合性（数値・訂正・撤回の伝播）
- `forecast-with-audit` — 監査つき予測（尺度規約・バイアス測定・識別検査・査読ラウンド）

## 常に守る規約

分析を書くとき、スキルを読む前から次の規約を守ること。

1. **派生値・定数をベタ書きしない。** スクリプトで計算した数値はソース（`scripts/params.py` など）から導出し、文書へは直接書き込まない。値が2箇所にあれば、どちらかが腐る。
2. **尺度規約を宣言する。** 数値を比較する前に、観測窓・集計対象の閾値・通貨基準・集計規則・周期上の位相を明示する。宣言し損ねた軸は必ず後で誤差になる。
3. **撤回・訂正は全文書へ伝播させる。** ある文書で撤回・訂正した主張や値は、それを引用する他の文書からも消す。下流の正典が更新されたのに上流が受け取っていない、という取り残しを残さない。
4. **主張に根拠の等級を付ける。** `[Fact]` / `[Observed]` / `[Company]` / `[Estimate]` / `[Hypothesis]` を区別する。仮説が後段で前提に化けることを防ぐ。
5. **収束を主張しない。** 検証・査読の途中経過を「検証し尽くした結果」と書かない。何ラウンド目で打ち切ったかを明示する。

詳細と手順は各スキルの `SKILL.md` を参照。
